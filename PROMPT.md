# Claude Code 인계 프롬프트

아래 내용을 WSL에서 `claude` 실행 후 그대로 붙여넣으세요.
작업 디렉터리: `/mnt/c/Users/canae/Desktop/steam_curator`

---

이 저장소는 LLM 부트캠프 4차 팀 프로젝트다. 먼저 README.md와 src/ 전체를 읽고 시작해줘.

## 프로젝트

**Steam Game Curator** — RAG + Function Calling 기반 게임 추천 에이전트. 최종 산출물은 Streamlit 웹앱.

3차 프로젝트(github.com/oseguri/LS_MINI_3)에서 Steam 인기 판매 게임 498건의 정형 데이터를 수집·검증·분석했다. 거기서 남은 한계가 출발점이다.

- "3만원 이하 액션 게임"은 SQL로 찾힌다
- "친구랑 둘이 낄낄대며 할 협동 게임"은 SQL로 못 찾는다

그래서 정형 조건은 **function calling**, 취향은 **의미 검색(RAG)**, 평판은 **유저 리뷰 근거**로 처리하고, 어떤 방식을 쓸지는 LLM이 질문을 보고 스스로 고르게 한다.

## 스택 (강의에서 배운 것 그대로)

- Gemini `client.interactions.create` (모델 `gemini-3.7-flash`, 임베딩 `gemini-embedding-2` 768차원)
- ChromaDB PersistentClient, cosine
- pydantic strict (`extra='forbid'`, Literal enum) 로 function call 인자 검증
- pandas, Streamlit, BeautifulSoup

## 현재 상태

수집 → 표준화 → 인덱싱 → 에이전트까지 스캐폴드가 있고 구문·검증 로직은 테스트를 통과했다. **데이터는 아직 하나도 수집 안 했다.**

### 먼저 고쳐야 할 것 3개

1. **`app/streamlit_app.py`가 없다.** 폴더 이동 중 유실됐다. README의 "Streamlit 화면" 섹션대로 새로 만들어야 한다. 탭 3개: 큐레이터 챗봇 / 게임 상세·리뷰 근거 / 데이터 대시보드. 챗봇 탭은 사이드바에 **LLM이 호출한 함수명·인자·검증 통과 여부·결과 건수**를 실시간 노출하는 게 핵심이다. `orchestrator.ask()`가 `{'answer', 'trace', 'results', 'interaction_id'}`를 돌려주니 그대로 렌더하면 된다.
2. **`pyproject.toml`이 python 3.14로 잡혀 있고 dependencies가 비어 있다.** chromadb·streamlit 휠이 3.14에 없을 가능성이 높으니 **3.12로 내리고** 의존성을 채워라: `requests`, `beautifulsoup4`, `pandas`, `python-dotenv`, `google-genai`, `chromadb`, `pydantic`, `streamlit`, `plotly`. uv를 쓰고 있으니 `uv add`로 넣어라.
3. **`main.py`는 uv가 만든 껍데기다.** 지우거나 CLI 진입점으로 바꿔라.

## 결정해야 할 것 — RAG 방식 (이게 제일 중요)

핵심 문제는 **어휘 불일치**다. 게임 소개문은 개발사가 쓴 마케팅 문구("신들의 힘을 빌려 지하 세계를 탈출하는 로그라이크")고, 사용자는 경험으로 검색한다("죽어도 계속 하게 되는 중독성 있는 게임"). 두 문장은 임베딩 공간에서 가깝지 않다. `short_description`만 임베딩하면 취향 검색이 헛돈다.

현재 코드는 컬렉션 2개(게임 설명 / 리뷰 청크)로 나뉘어 있고 게임 검색은 설명 임베딩을 쓴다. 이걸 바꾸려 한다.

### 채택하려는 방식: 리뷰 개별 임베딩 + 게임 단위 집계

```
쿼리 "혼자 조용히 몰입되는 게임"
  → 리뷰 컬렉션에서 top 100 검색   (리뷰 1건 = 문서 1개, 합치지 않는다)
  → app_id 별로 그룹핑
  → 게임 점수 = 매칭된 리뷰 상위 3개의 유사도 평균
  → 상위 5개 게임 추천 + 매칭된 리뷰를 추천 근거로 함께 반환
```

**리뷰를 이어붙여 게임당 벡터 하나로 만들면 안 된다.** 임베딩이 평균을 내서 개별 특징이 씻겨나가고, 모든 게임 벡터가 서로 비슷해져 순위가 안 갈린다. 토큰 한도도 넘긴다.

이 방식의 이점:
- 소수 신호도 잡힌다 ("무섭다"는 리뷰가 3건뿐인 게임도 그 쿼리에서 걸린다)
- **추천 근거가 공짜로 따라온다** — 왜 추천했는지가 곧 매칭된 리뷰다
- 컬렉션 하나로 추천과 리뷰 근거를 둘 다 커버한다

집계 주의: 합계는 리뷰 많은 게임이 무조건 이기고, 최대값은 튀는 리뷰 하나에 휘둘린다. **상위 3개 평균**을 쓰고, 매칭된 리뷰 개수를 신뢰도로 함께 반환해라.

게임 설명 컬렉션은 **폴백으로 남긴다** — 리뷰가 없는 신작·비인기작은 리뷰 검색에 안 잡히기 때문이다.

### threshold를 용도별로 다르게

지금 코드는 게임 검색과 리뷰 질의에 둘 다 threshold를 거는데 이건 틀렸다.

- **추천(게임 검색)**: 상대 순위가 중요하지 절대 유사도가 중요한 게 아니다. threshold를 낮추고(0.3 근처) 대신 유사도가 낮으면 "확신이 낮다"고 답변에 밝혀라.
- **리뷰 질의**: threshold가 환각 방지의 전부다. 엄격하게 유지하고, 미달이면 생성 호출 자체를 하지 마라.

**임계값은 찍어보고 정해라.** 한국어 짧은 문장은 코사인 유사도가 좁은 구간에 몰린다. 인덱싱 후 관련/무관 질문 쌍의 유사도 분포를 출력하는 스크립트를 만들고 그 결과로 값을 정해라. 지금 config의 0.65/0.55는 근거 없는 추정치다.

### 리뷰 청킹 재검토

400자 고정 청킹은 리뷰에 안 맞다. 스팀 리뷰는 대부분 100~300자라 안 잘리고, 잘리는 긴 리뷰는 "근데 최적화가"에서 끊겨 문맥이 깨진다.

- **1리뷰 = 1문서**로 두고 1,000자 넘는 것만 문장 경계에서 분할
- 전처리: 30자 미만 제외, 같은 문자 비율 30% 넘는 것 제외(`ㅋㅋㅋ` 도배·ASCII 아트)
- 재순위에 플레이타임 반영 검토: `유사도 0.7 + 정규화 플레이타임 0.15 + 리뷰 추천수 0.15`

**작업 시작 전에 이 RAG 설계를 어떻게 반영할지 계획을 먼저 보여주고 확인받아라.**

## 데이터 수집

`crawl_list` → `fetch_details` → `standardize` → `fetch_reviews` → `standardize` → 인덱싱 순서다.

- 게임 1,500~2,000건 (인기순 500 + 장르별 각 200~250, 중복 제거)
- 리뷰는 상위 300개 게임 × 100건, **긍정 50 + 부정 50으로 균형 수집** (긍정이 압도적으로 많아 최신순으로 받으면 요약이 항상 긍정으로 기운다)

### 실측 기반 주의사항

- `appdetails`는 **1회 호출당 appid 1개**만 안정적이다. 배치로 넣으면 일부 null이 온다.
- 요청 사이 **1초 이상 대기 필수**. 없으면 429 이후 IP 단위로 잠시 막힌다.
- `price_overview`는 100배 정수다(₩27,500 → 2750000). **첫 표준화 결과에서 가격 자릿수를 반드시 눈으로 확인해라.** 3차 프로젝트에서 정규식 오타로 모든 가격이 0이 나온 적이 있고, 여기가 같은 자리다.
- 개별 리뷰에 별점은 없다. `voted_up` 추천/비추천 2단계뿐이고, `review_score` 1~9점은 게임 단위 집계값이라 `appreviews`의 `query_summary`로만 얻을 수 있다.
- `config.GENRE_FILTERS`의 Steam 장르 태그 ID는 미검증이다. Steam이 종종 바꾸니, 장르별 수집 결과가 인기순과 거의 같으면 태그가 안 먹은 것이다.
- `type`이 `game`이 아닌 것(DLC·사운드트랙·데모)은 제외한다.
- `embed_content`의 배치 입력을 SDK가 거부하면 단건 반복으로 바꾸고, 인덱싱 시간이 늘어난다는 걸 알려줘라.

## 검색 품질 평가 (발표의 핵심 재료)

질문 20개에 "이 게임이 정답" 라벨을 붙이고 **Recall@5**를 재는 스크립트를 만들어라. 임베딩 재료를 바꿀 때마다 이 숫자가 어떻게 변하는지가 발표에서 제일 강력하다.

> 설명만 → 0.35 / 리뷰 집계 → 0.7x

`src/eval/`에 이미 툴 라우팅 정확도와 인자 검증 차단율 평가가 있으니 같은 자리에 추가해라.

## 코드 스타일

강의 예제 스타일을 따른다. 팀원들이 배운 코드와 이어져야 읽힌다.

- 경로·모델·상수는 전부 `config.py` 한 곳에서 관리
- 함수는 작게, 타입 힌트 명시
- function 선언은 `{'type': 'function', 'name': ..., 'description': ..., 'parameters': {...}}` dict
- `TOOLS` 리스트와 `FUNCTION_MAP` dict로 등록, `validate_function_arguments()`로 검증 후 실행
- 툴 함수는 `{'success': bool, ...}` 형태 dict 반환
- 주석은 한국어로, "무엇을"이 아니라 "왜 이렇게 했는지"를 적는다
- 커밋 메시지도 한국어

## 지금 해야 할 순서

1. README.md와 src/ 전체 읽기
2. pyproject 파이썬 버전·의존성 정리, uv 환경 구성
3. RAG 설계 반영 계획을 제시하고 확인받기
4. 수집 실행 (crawl_list → fetch_details → standardize) 후 **가격·장르 자릿수 눈으로 검증**
5. 리뷰 수집 → 표준화 → 인덱싱
6. 유사도 분포 출력해서 threshold 확정
7. `app/streamlit_app.py` 작성
8. 평가 스크립트 실행, 결과를 README에 기록
